#include "map.h"			// t_map, t_coord_d
#include <stdbool.h>		// bool: true, false

// Given a starting point, returns true if the 'map' is closed using the
// flood-field algorithmn. If the map is not closed, returns false.
bool	map_is_closed(t_map *m, t_coord_d *coord)
{
	// TODO: Implement this function.
	++m;
	++coord;
	return (true);
}
